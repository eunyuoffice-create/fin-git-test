version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
        - cp -r .next/standalone/. .next/standalone-app
        - cp -r .next/static .next/standalone-app/.next/
        - cp -r public .next/standalone-app/
  artifacts:
    baseDirectory: .next/standalone-app
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
