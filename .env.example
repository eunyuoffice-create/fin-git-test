# ===========================================
# 환경 변수 템플릿 (고객사 전달용)
# ===========================================
# 이 파일을 복사하여 각 환경에 맞게 설정하세요:
# - .env.local (로컬 개발)
# - .env.development (개발 서버)
# - .env.production (참고용, 실제는 Amplify Console에서 설정)
# ===========================================

# ------------------------------------------
# Slack Webhook URL (필수)
# ------------------------------------------
# Slack 워크스페이스에서 Incoming Webhook 생성 후 URL 입력
# 예: https://hooks.slack.com/services/T00000000/B00000000/XXXXXXXXXXXXXXXXXXXX
SLACK_WEBHOOK_URL=

# ------------------------------------------
# CORS 허용 도메인 (필수)
# ------------------------------------------
# 쉼표로 구분하여 여러 도메인 설정 가능
#
# 로컬 개발:
# ALLOWED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080
#
# 프로덕션:
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=

# ------------------------------------------
# 사이트 URL (선택)
# ------------------------------------------
# Next.js 앱의 공개 URL
#
# 로컬: http://localhost:8080
# 프로덕션: https://yourdomain.com
NEXT_PUBLIC_SITE_URL=

# ------------------------------------------
# Node.js 버전 (Amplify용)
# ------------------------------------------
# Amplify Build 설정에서 사용할 Node.js 버전
# Next.js 14는 Node.js 18.17 이상 필요
# 권장: 18 (LTS, 안정적)
NODE_VERSION=18

# ===========================================
# 📝 설정 가이드
# ===========================================

# 1. 로컬 개발 (.env.local)
# ---------------------------
# cp .env.example .env.local
#
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
# ALLOWED_ORIGINS=http://localhost:8080
# NEXT_PUBLIC_SITE_URL=http://localhost:8080
# NODE_VERSION=18

# 2. 프로덕션 (AWS Amplify Console)
# ----------------------------------
# Amplify Console → Environment variables에서 직접 설정:
#
# SLACK_WEBHOOK_URL → 프로덕션용 Webhook URL
# ALLOWED_ORIGINS → https://yourdomain.com
# NODE_VERSION → 18

# ===========================================
# ⚠️ 보안 주의사항
# ===========================================
# 1. 이 파일은 Git에 커밋하지 마세요! (.gitignore에 포함됨)
# 2. SLACK_WEBHOOK_URL은 외부에 노출하지 마세요
# 3. 프로덕션 환경 변수는 Amplify Console에서만 설정하세요

# ===========================================
# 📚 참고 문서
# ===========================================
# - 배포 가이드: docs/DEPLOYMENT.md
# - 보안 가이드: docs/SECURITY.md
# - 인수인계 문서: docs/HANDOVER.md
